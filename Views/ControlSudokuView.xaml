<catel:UserControl x:Class="Sudoku.Views.ControlSudokuView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://schemas.catelproject.com"
                   xmlns:views="clr-namespace:Sudoku.Views">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding NewGameCommand}">New Game</Button>

        <WrapPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Label>Mode:</Label>
            <ComboBox
                ItemsSource="{Binding ControlSudokuModes}"
                DisplayMemberPath="Value"
                SelectedValuePath="Key"
                SelectedValue="{Binding ControlSudokuMode, Mode=TwoWay}" />
        </WrapPanel>

        <views:SudokuGridPartView Grid.Row="2" Grid.Column="0" DataContext="{Binding ControlGrid}" />
    </Grid>
    
</catel:UserControl>
